apiVersion: v1
kind: Service
metadata:
  name: golang-gitops-project
spec:
  selector:
    app: golang-gitops-project
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 31803
  type: NodePort
